version: '3.8'
services:
  back:
    image: debian
    command: echo "Hello there!!"
    depends_on:
      - db
    networks:
      - backnet
  db:
    image: nginx:debian
    networks:
      - backnet
    volumes:
      - data:/test
  front:
    image: nginx:debian
    networks:
      - frontnet
networks:
  frontnet:
  backnet:
volumes:
  data:
